# SMTP Configuration for email sending
SMTP_HOST=your_smtp_server
SMTP_PORT=587
SMTP_USER=your_email
SMTP_PASS=your_password
SMTP_FROM=your_sender_email

# Contact form recipient
CONTACT_RECEIVER=recipient_email

# Server port
PORT=3000